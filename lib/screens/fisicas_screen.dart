  
import 'package:flutter/material.dart';

class FisicasScreen extends StatelessWidget {
   
  const FisicasScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('FisicasScreen'),
      ),
     );
  }
}